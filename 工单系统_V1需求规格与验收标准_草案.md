# 工单系统 V1 需求规格与验收标准（草案）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 0.7（恢复后规则修订稿） |
| 编写日期 | 2026-09-03 |
| 规则确认日期 | 2026-09-06；停用与转派边界于 2026-09-10 修订确认 |
| 内容恢复日期 | 2026-09-10 |
| 状态 | 核心规则及本轮修订已确认；实施默认值待技术设计细化，验收尚未执行 |
| 适用范围 | 单企业内部 IT 问题报修、人工分配与处理 |
| 项目目标 | 独立完成一个小而完整的全栈系统，跑通创建、分配、处理、解决、确认关闭的业务闭环 |

> 恢复说明：本地文件出现 0.1 版内容，与现存引用 0.6.1 的页面评审记录不一致。本稿依据此前确认记录和《工单系统 V1 页面设计与评审记录》重建，不是找到了逐字节一致的历史文件。恢复前原文件已原样保存在本地文档备份目录。
>
> 本稿恢复已确认规则，不引入新的业务范围。数量、长度、模板等实施默认值单列，不能理解为每一项均已获得单独确认。文档、交互草图检查和真实系统验收是不同层次，不能互相代替。
>
> 建议先看第 1、2、3、8 节，再按需求编号核对验收标准。

## 1. 目标、范围与完成定义

### 1.1 项目目标

员工通过网页提交 IT 问题，管理员受理并人工分配给工程师；工程师开始处理、通过回复补充沟通、提交解决说明；申请人实际确认问题解决后关闭。申请人无法确认时，管理员可以在已解决状态填写理由代关，明确记录实际关闭方式。

V1 验证完整业务闭环、数据访问权限、状态规则、持久化与可追溯性，不追求成熟商业产品的功能数量。由项目负责人亲自搭建框架、编写核心业务；设计共同讨论，成熟通用组件可复用，不自行设计密码算法。

### 1.2 纳入 V1

- 账号登录、退出、首次及重置后改密、自行改密。
- 管理员单个创建账号，Excel 批量创建员工，重置密码与停用账号。
- IT 工单创建、查询、分类、普通/高/低优先级。
- 管理员人工分配和转派，工程师开始处理。
- 申请人、当前负责人、管理员追加回复；可选截图。
- 解决说明、未解决反馈、申请人确认关闭、管理员有理由代关。
- 符合条件的取消；完整操作留痕；终态只读。
- 后端权限校验、事务与并发保护、持久化及必要测试。

### 1.3 暂不纳入

不做非 IT 业务、通用工作流或审批引擎、自助注册、统一身份接入、邮件找回密码、可配置角色、自动抢单/派单、自动关闭、SLA 升级、知识库、站内推送/邮件/短信/企微通知、评分问卷、AI/Agent/RAG、租户隔离、资产备件、工单合并、私密备注、工单删除和终态重开。

附件只收图片，不收日志、PDF、Office 文档。登录故障属于 IT 问题；账号授权申请及其审批、对外部系统自动开通权限不属于本期。

### 1.4 完成定义

能在说明的演示环境运行，真实账号可以走完主线及取消路径，刷新和重启后数据仍在。第 5 节验收项执行并记录结果，关键权限、状态、事务、并发及文件安全测试通过，运行说明齐全。

不以“页面能点击”或“启动成功”代替完整交付；未测的验收项保持未验证。生产可用性、容量和部署保障不在本稿中无依据承诺，环境与测试数据规模在技术设计中明确。

## 2. 工单生命周期

### 2.1 状态定义

底层共五种状态；页面将 OPEN 分成待分配、待开始，所以可呈现六种情况，不增加第六个状态枚举。

| 状态 | 中文含义 | 负责人及含义 |
| --- | --- | --- |
| OPEN | 待处理 | 负责人为空显示待分配；已有负责人显示待开始。两者是筛选条件，不另设状态 |
| PROCESSING | 处理中 | 当前负责人已开始，可追问、继续处理或提交解决说明 |
| RESOLVED | 已解决，待确认 | 已提交本轮解决说明，等待申请人确认或管理员有理由代关 |
| CLOSED | 已关闭 | 申请人确认或管理员代关完成，整单只读 |
| CANCELLED | 已取消 | 按合法条件终止，包括停用申请人已无需服务；不是已解决，整单只读 |

不设置 WAITING_USER，不做等待/恢复处理按钮。追问、申请人回复不改变状态，不阻断工程师继续处理。申请人撤回意愿也是普通回复，不增加取消审批状态。

### 2.2 状态转换与条件

| 当前状态 | 动作 | 操作者 | 结果与必备条件 |
| --- | --- | --- | --- |
| 无 | 创建 | 任意有效登录账号 | OPEN；本人为申请人；无负责人 |
| OPEN | 分配 | 管理员 | OPEN；指定有效工程师 |
| OPEN | 转派 | 管理员 | OPEN；指定不同的有效工程师并填理由 |
| OPEN | 开始处理 | 当前负责人 | PROCESSING；记录曾开始处理事实 |
| PROCESSING | 转派 | 管理员 | OPEN；新负责人重新开始，理由必填 |
| PROCESSING | 提交解决 | 当前负责人 | RESOLVED；解决说明必填 |
| RESOLVED | 反馈未解决 | 申请人 | PROCESSING；理由必填，负责人不变 |
| RESOLVED | 确认关闭 | 申请人 | CLOSED；记录本人确认 |
| RESOLVED | 代关 | 管理员 | CLOSED；记录实际管理员、方式和必填理由 |
| OPEN | 自行取消 | 申请人 | CANCELLED；从未开始处理，理由必填 |
| OPEN / PROCESSING | 按条件取消 | 管理员 | CANCELLED；合法类别及具体理由 |
| CLOSED / CANCELLED | 任何写操作 | 所有人 | 拒绝；仅保留有权阅读 |

未列出的状态转换一律不允许。RESOLVED 不能直接转派或取消；需申请人真实反馈未解决才能回到处理。已关闭不重开，确有新问题应新建工单，不覆盖过去记录。

管理员只能转派 OPEN、PROCESSING 工单，理由必填并留痕；理由由管理员对事实负责，不新增审批或自动判断理由合理性的功能。未关闭不等于尚未解决：工程师 A 已提交解决结果后，即使申请人迟迟未确认或 A 已停用，管理员也不能将 RESOLVED 转给 B。核实已解决后可有理由代关，解决人仍是 A，关闭人是实际管理员；只有申请人反馈未解决回到 PROCESSING 后才可转派，原解决记录不覆盖。

### 2.3 取消条件

1. 申请人撤回：从未开始处理的 OPEN 可自行取消；开始过后，由申请人在回复中提出，管理员核实并取消。
2. 重复：管理员填写保留工单编号；关联单必须存在、不是自身且未取消；不修改另一单或扩大阅读权限。
3. 非 IT 范围：管理员说明原因及建议联系部门/渠道。
4. 申请人停用且无需服务：管理员核实后说明原因，只适用于 OPEN、PROCESSING，无需停用账号回复；仍需服务则继续解决后代关。

问题困难、描述不清、暂未回复本身不是取消依据。不用程序假装判断理由是否真实；后端校验类别和结构条件，管理员对实际判断负责。取消记录含操作者、时间、类别、理由和适用的关联编号；原负责人、回复、历史均保留，不制造解决说明或关闭记录。

## 3. 角色、数据范围与操作权限

每个账号一个固定角色：员工、工程师或管理员。“申请人”是该工单与账号的关系，不是第四种角色；三种角色都可提交自己的 IT 问题。

| 身份 | 阅读范围 | 非终态动作 |
| --- | --- | --- |
| 申请人 | 本人提交的工单 | 回复；符合条件的自行取消；RESOLVED 确认或反馈未解决 |
| 当前负责工程师 | 当前负责的工单，以及本人申请/曾负责的工单 | 对当前负责工单回复、开始或提交解决；按状态约束 |
| 历任负责人 | 曾负责整单，包括转派后的新增回复、图片和历史 | 仅凭旧负责人关系不能写；同时为申请人时保留申请人动作 |
| 管理员 | 全部工单 | 回复、分类优先级调整、分配转派、符合条件取消、已解决后有理由代关 |
| 停用账号 / 未登录者 | 无 | 无 |

工程师可处理自己申请的工单，并保留申请人动作。管理员自己申请时，必须区分“申请人确认”与“管理员代关”，不能仅凭角色推断方式。管理员不能冒充工程师提交解决。当前负责人在 RESOLVED 可回复，但不能重复解决或以负责人身份直接关闭。

CLOSED 和 CANCELLED 对所有人只读，包含管理员。原始标题和描述创建后不改，纠正通过回复追加；所有有权阅读者均可见回复，无内部私密备注。后端依据当前会话、最新账号状态、工单关系和状态执行校验，隐藏按钮不是安全措施。

## 4. 业务规则

| 编号 | 规则 |
| --- | --- |
| BR-01 | 一个工单最多一个当前负责人；创建时为空，分配/转派时只能选择未停用工程师；负责人随后停用不清除原关联或历史署名 |
| BR-02 | 仅 OPEN、PROCESSING 可转派，理由必填；处理中转派回 OPEN；RESOLVED 与终态禁止转派；旧负责人整单只读，申请人身份例外 |
| BR-03 | 原始标题、描述不可修改；管理员可调整非终态分类/优先级（含 RESOLVED），记录前后值 |
| BR-04 | 追问复用普通回复，不增加等待状态；非终态允许申请人、当前负责人和管理员追加回复 |
| BR-05 | 解决必须填写说明；申请人未解决退回必须说明理由；各轮结果追加保留 |
| BR-06 | CLOSED 不重开、不继续处理、不删除；任何人不能改历史 |
| BR-07 | 仅 RESOLVED 可关闭；管理员代关必填理由，保存真实操作者与关闭方式，无自动关闭 |
| BR-08 | 所有接口按有效会话及当前权限校验；申请人身份不信任客户端传值，停用账号不能继续访问 |
| BR-09 | 业务变更及留痕原子保存；旧状态、重复动作与并发冲突拒绝，不静默覆盖或追加虚假成功记录 |
| BR-10 | 管理员单个创建账号、Excel 批量创建员工；临时密码首次必改；账号停用而不删除 |
| BR-11 | 申请人只可自行取消从未处理的 OPEN；管理员按撤回、重复、非 IT 范围或 BR-13 的停用无需服务条件取消 |
| BR-12 | CANCELLED 为只读终态；保存取消类别及理由，不清除原数据，也不冒充解决或关闭 |
| BR-13 | 工程师有未结束负责工单也可停用；停用不自动改状态、负责人或历史，由管理员按原规则人工转派或收尾，不增加自动交接/审批；申请人停用后仍需服务继续解决代关，无需服务可有理由取消 |
| BR-14 | 图片限创建/回复附加，提交后不可修改删除；读取和绑定均遵循工单权限，终态不允许新附件 |

### 4.1 实施默认值（便于后续实现，不是新增业务范围）

以下数值和模板作为初始实现建议；正式编码前可调整，但必须同时更新对应验收项，不能只改实现。

- 标题去除首尾空白后 1–100 个 Unicode 码点；描述、回复、解决说明、退回及其他理由为 1–5000 个码点的非空纯文本。
- IT 分类固定为网络、账号登录故障、软件、硬件、其他 IT；未来业务类型与此分类分开。
- 优先级低、普通、高，创建默认普通，仅管理员调整；不绑定 SLA。
- 编号唯一且稳定，格式留给技术设计；时间以服务端记录为准，申请人由会话确定。
- 列表显示编号、标题、分类、优先级、状态、申请人、负责人、更新时间。支持编号精确查询、标题关键词、状态/分类/优先级筛选；管理员可按负责人筛选。
- 默认每页 10 条，按创建时间倒序并以唯一编号稳定排序。待分配、待开始由 OPEN 与负责人字段组合；曾负责不计为当前待办。
- 操作后刷新当前数据；他人操作先使用手动刷新，不增加消息推送。提交失败保留表单；创建结果不明时先查询，不静默重试。前端防重复点击不替代后端校验；通用幂等机制由技术设计明确，不在此宣称已实现。
- Excel 为 .xlsx，模板工号、姓名必填，邮箱可选；工号按文本处理并唯一，保留前导零，邮箱不是身份证明。每文件最多 500 个员工、5 MiB。拒绝公式、非法角色、重复及冲突，整批校验和事务导入，不部分创建、不覆盖既有账号。
- 每账号独立随机临时密码，一次性交付本次操作管理员，由管理员通过既有安全渠道分发；密码不可回查，遗失只能重置。敏感下载文件妥善分发后清理，不进仓库。初始管理员初始化不能覆盖既有账号；不提供自助注册、账号删除、角色修改或停用恢复入口。
- 工程师停用不以名下工单全部结束为前提，旧会话失效，原工单在管理员列表中保留，由管理员人工跟进。OPEN、PROCESSING 可填写理由转派；RESOLVED 保持待确认，只能确认关闭、管理员有理由代关，或申请人反馈未解决后再转派。无需服务的工单仍须满足原取消条件，工程师停用本身不是新的取消条件。不增加自动交接或交接审批。禁止停用自己及最后一个有效管理员。
- 单次创建/回复最多 5 张 PNG/JPG/JPEG，每张最多 5 MiB（5×1024×1024 字节）。正文仍必填，图像需服务端真实格式校验。提交前可移除，提交后不可替换/删除。
- 临时上传须绑定正确上传者及最终工单/回复，不能盗用其他上传；附件不可公开裸链绕过权限，终态仍可授权阅读。临时文件回收策略在技术设计中落实。账号 Excel 导入和工单附件是不同接口。
- 账号创建/导入/重置/停用有独立账号审计，记录实际管理员、时间、目标和结果，不把账号审计混进工单历史；敏感凭据不写日志。

## 5. 功能需求与验收标准

共 11 项需求、75 项验收标准。以下均为待执行标准，不代表实现完成或测试通过。测试时建立不同员工 A/B、工程师 E1/E2、管理员 M 和停用账号，分别验证页面行为与直接接口请求。

### REQ-01 登录与访问入口

- AC-01-01：有效账号使用正确密码登录后进入对应工作区；临时密码账号须先完成 REQ-10 要求的改密。
- AC-01-02：密码错误、账号不存在或账号已停用时，统一提示登录失败，不返回业务数据。
- AC-01-03：未登录请求工单列表、详情及写操作时，后端拒绝访问。
- AC-01-04：退出登录后，原会话不能继续读取或修改业务数据。

### REQ-02 创建 IT 工单

- AC-02-01：申请人 A 提交有效内容后，生成唯一工单编号；申请人为 A，状态 OPEN，负责人为空，优先级普通，并保存创建记录。
- AC-02-02：标题、描述为空或超长，或问题类型不合法时拒绝提交，不产生工单及创建历史。
- AC-02-03：客户端伪造申请人 B、指定负责人或初始 CLOSED 等受保护字段时，后端拒绝非法赋值，不能创建越权结果。
- AC-02-04：创建成功后刷新页面或查询结果不会再次创建工单；不能用页面刷新触发重复提交。

### REQ-03 工单可见范围与列表

- AC-03-01：普通员工仅可看本人提交的工单；工程师可看当前负责、曾负责及本人提交的工单；仅管理员具有全量阅读权限。申请人是工单关系，不限制账号通过其他合法身份获得的权限。
- AC-03-02：账号 A 既不是目标工单申请人，也不是当前或历任负责人，且不是管理员时，直接请求该单详情、回复或历史均返回统一的不存在或无权访问响应；有其他合法授权关系时不能仅因不是申请人而拒绝访问。
- AC-03-03：搜索、筛选、分页总数均限制在当前账号有权访问的数据中。
- AC-03-04：存在 25 条可见记录时，默认每页 10 条，依次显示 10、10、5 条；排序及总数正确，翻页不重复、不遗漏。
- AC-03-05：从 E1 转派给 E2 后，E1 可只读整个工单，包括 E2 后续回复、图片和历史，不能以旧负责人身份操作；若 E1 同时是申请人，保留申请人权限；停用后不再可读；终态对所有人只读。

### REQ-04 分配、转派与分类

- AC-04-01：管理员将 OPEN 工单分配给有效工程师 E1 后，状态仍为 OPEN，保存实际操作者、时间和负责人前后值。
- AC-04-02：员工或工程师调用分配接口被拒绝，不能通过前端隐藏按钮以外的途径越权分配。
- AC-04-03：分配给不存在、已停用或非工程师账号时拒绝，不改变负责人。
- AC-04-04：PROCESSING 工单由 E1 转派给 E2 时必须填理由，状态回到 OPEN；保留旧记录，E1 只读，E2 需要开始处理。
- AC-04-05：OPEN、PROCESSING 转派缺少理由或转派给当前负责人时拒绝；RESOLVED、CLOSED、CANCELLED 无论是否填写理由均不能转派。A 已解决但申请人未确认时，管理员尝试转给 B 被拒绝，A 停用也不能绕过；原负责人、解决人和记录不变。
- AC-04-06：管理员可调整非终态工单的问题类型、优先级并记录前后值；员工和工程师不能修改。

### REQ-05 开始处理与沟通

- AC-05-01：当前负责人对 OPEN 工单点击开始处理后进入 PROCESSING，并保存处理开始记录。
- AC-05-02：非当前负责人开始处理，或对已处于 PROCESSING 的工单重复开始时拒绝，不追加虚假成功历史。
- AC-05-03：PROCESSING 中工程师通过普通回复追问，保存作者、时间和正文，状态不变；即使申请人尚未回复，工程师仍可继续回复或在实际解决后提交解决说明。
- AC-05-04：申请人回复后状态保持不变，不出现继续处理或恢复处理的必经操作。
- AC-05-05：申请人、当前负责人、管理员均可在非终态追加有效回复；原始标题、描述和已提交回复不可改写。
- AC-05-06：无权人员回复、空白或超长回复、修改或删除已提交回复时拒绝。

### REQ-06 解决、确认与关闭

- AC-06-01：当前负责人在 PROCESSING 填写解决说明后进入 RESOLVED，记录实际处理人、时间和说明，不自动关闭。
- AC-06-02：解决说明空白、非当前负责人提交，或从 OPEN 直接提交解决时拒绝。
- AC-06-03：申请人在 RESOLVED 填写未解决理由后退回 PROCESSING，负责人不变，保留此前解决说明及本次反馈。
- AC-06-04：工程师再次提交解决时追加新一轮说明，历次解决和退回记录均保留，不覆盖。
- AC-06-05：申请人在 RESOLVED 确认问题解决后关闭，记录实际确认人、时间及申请人确认关闭方式。
- AC-06-06：管理员在 RESOLVED 填写代关理由后关闭，记录实际管理员、时间、管理员代关方式及理由；不冒充申请人满意确认。A 提交解决后被停用，管理员代关仍保留 A 为该轮解决人，不将处理归属改为管理员或 B。
- AC-06-07：管理员不填理由、非申请人工程师执行关闭，或从其他状态直接关闭时拒绝。
- AC-06-08：CLOSED 工单禁止回复、修改分类优先级、转派、重新打开及删除；仍允许有权人员阅读。
- AC-06-09：PROCESSING 长期未收到回复或 RESOLVED 长期未确认时，不因定时任务自动改变状态。
- AC-06-10：管理员代关理由空白时不落库；关闭历史保存失败时关闭状态及相关记录全部回滚。

### REQ-07 留痕、权限与并发一致性

- AC-07-01：创建、分配、开始、追问、回复、解决、未解决退回、再次解决和关闭的完整路径可追溯实际作者、时间、动作、状态或负责人变化及必填理由；普通回复不伪造状态变更。
- AC-07-02：历史只能由有权访问工单的人读取，任何角色不能修改或删除历史。
- AC-07-03：模拟历史保存失败时，对应业务变更一并回滚，不出现有状态无记录。
- AC-07-04：两名管理员基于同一旧版本同时分配时至多一项成功，另一项提示冲突并要求刷新，不静默覆盖。
- AC-07-05：转派后旧负责人使用旧页面提交解决等操作时，后端按最新负责人和状态拒绝。

### REQ-08 持久化、安全与交付

- AC-08-01：重启服务后，账号、工单、回复、历次解决、附件关联及操作历史仍存在，权限和状态一致。
- AC-08-02：按运行说明可以初始化并启动系统、建立验收账号和执行验收，不依赖未说明的手工数据库修改。
- AC-08-03：密码以安全哈希保存；代码仓库及日志不出现真实密钥、明文密码或完整认证凭据。
- AC-08-04：标题、描述、回复和理由中的脚本或 HTML 按普通文本安全展示，不执行脚本。
- AC-08-05：自动化测试覆盖关键状态转换、越权、字段校验、事务回滚和并发冲突；失败时不能标记交付验收通过。

### REQ-09 取消工单

- AC-09-01：申请人可对从未开始处理的 OPEN 工单填写理由后取消，未分配或已分配均可；保存取消信息，原负责人和内容保留。
- AC-09-02：PROCESSING 工单由申请人回复表达撤回意愿后，管理员核实并填理由取消，保留申请人回复及管理员操作记录。
- AC-09-03：管理员按重复工单取消时，须指定实际存在、非当前单且未取消的保留工单；只取消当前工单，不修改另一单，也不扩大访问权限。
- AC-09-04：管理员按不属于 IT 服务范围取消时，须填写原因及建议联系的部门或渠道，申请人可阅读。
- AC-09-05：取消理由空白，重复类别缺少关联编号，或关联自身、不存在或已取消工单时拒绝。
- AC-09-06：无权人员、非申请人工程师不能取消；申请人不能自行取消 PROCESSING，或曾经处理后转派回 OPEN 的工单。
- AC-09-07：非法取消类别、以描述不清/问题困难/未回复作为取消条件，或对 RESOLVED、CLOSED、CANCELLED 执行取消均不允许；理由真实性由管理员人工核实，系统不声称能判断自然语言事实。
- AC-09-08：CANCELLED 禁止回复、修改、分配、转派、开始、解决、关闭、重开和删除；有权人员仍可阅读全部保留记录。
- AC-09-09：取消历史保存失败时全部回滚；取消和开始处理并发时至多一项成功，另一项按最新状态失败。
- AC-09-10：取消结果在详情及筛选中明确显示，不计入待办，不伪造解决说明或关闭记录。

### REQ-10 账号管理与 Excel 导入

- AC-10-01：管理员可单个创建员工、工程师或管理员；批量 .xlsx 只创建员工，工号前导零保留，独立临时密码及账号操作审计正确。
- AC-10-02：非管理员调用账号创建、导入、重置、停用及管理列表接口均被拒绝，不返回账号清单或改变密码。
- AC-10-03：缺少必填项、文件内重复工号、与现有账号重复（含停用账号）、非法角色或公式单元格时整批拒绝并报告行号错误；重复导入不覆盖账号或重置密码。
- AC-10-04：CSV、XLS、XLSM、伪装 XLSX、超大小或超行数文件被拒绝；边界内有效文件通过。具体上限采用本文件实施默认值，调整须同步标准。
- AC-10-05：首次登录和管理员重置后的临时密码登录，仅可进行必要的改密及退出操作；改密前不能访问业务，改密后旧密码失效。
- AC-10-06：自行改密须验证原密码；管理员重置使旧会话失效，生成新临时密码并强制改密，日志不含密码或认证凭据。
- AC-10-07：工程师 E1 名下分别存在 OPEN、PROCESSING、RESOLVED 工单时仍可停用，旧会话失效，原状态、负责人及历史不自动改变，管理员仍可查询跟进。前两种可填写理由转派给 E2，E2 重新开始；RESOLVED 不可直接转派，可按原关闭规则收尾，或申请人说明未解决退回 PROCESSING 后再有理由转派。无自动交接或额外审批。
- AC-10-08：停用作为申请人的员工可成功，其旧会话失效；工单申请人、当前状态和历史不自动改变。
- AC-10-09：停用申请人的 OPEN 或 PROCESSING 工单，经管理员核实已无需服务，可填写具体理由取消，无需停用账号再次回复；有效申请人不能套用此专用类别。
- AC-10-10：停用申请人仍需服务的工单继续处理，实际解决后进入 RESOLVED，再由管理员有理由代关，不能跳过解决或伪造员工确认。
- AC-10-11：拒绝停用自己或最后一个有效管理员；V1 不提供账号删除和角色修改。
- AC-10-12：停用与分配/转派并发时按一致顺序生效：停用先完成则拒绝新增分配；分配先完成则允许随后停用并保留工单供管理员人工跟进，不能回滚停用或自动结束工单。并发重复导入不能产生重复账号，单批仍为全有或全无。
- AC-10-13：临时凭据仅向本次操作管理员一次性交付，其他账号不能读取；后续不可查询原密码，数据库仅保存哈希，日志不保存明文。

### REQ-11 工单截图附件

- AC-11-01：创建或回复可不附图，也可提交有效 PNG/JPG/JPEG；图片关联到正确工单及对应正文，作者和时间正确。
- AC-11-02：每次最多 5 张、每张最多 5×1024×1024 字节的边界通过；第 6 张或超限图片后端拒绝，不静默丢图后提交。
- AC-11-03：日志、PDF、Excel、仅改扩展名的伪图片或损坏图片被拒绝，不能只依赖扩展名或客户端 MIME。
- AC-11-04：未登录、无权访问或已停用账号，即使复制图片链接也无法预览或下载。
- AC-11-05：提交前可移除所选图片；提交后不允许删除或替换，终态不能新增附件，但有权人员仍可查看原图。
- AC-11-06：绑定他人临时上传或另一工单附件的请求被拒绝，不能借附件接口突破工单权限。
- AC-11-07：上传失败、提交失败或提交前工单已进入终态时，明确提示并保留可重试的表单内容，不宣称缺图提交成功；临时孤立文件有清理方案。

## 6. 页面与交互依据

页面和草图的具体评审见同目录《工单系统 V1 页面设计与评审记录》。本稿约束行为，评审稿组织页面；发生冲突先澄清并同步，不以草图覆盖需求。

入口包括登录/改密、我的工单、工程师处理工作台、管理员工单管理、创建、共享详情及账号管理。详情同时保留原始描述、沟通、历次解决和操作历史；按当前状态及身份展示可执行动作，终态只展示结果与只读内容。

“待我确认”表示本人申请且已解决；不是等待补充信息。回复双方可继续沟通，没有专门等待用户状态。管理员代关应直观显示实际管理员和理由，不能呈现为申请人满意。

## 7. 为后续类型扩展保留边界

| 编号 | 设计与验收要求 |
| --- | --- |
| AR-01 | 区分未来的业务类型与 IT 问题分类；V1 只落地 IT，不把软件/网络等当成跨业务类型 |
| AR-02 | 将账号权限、工单主流程、回复、历史、附件与 IT 特定分类规则合理分离，避免分类判断散落；不实现通用工作流引擎 |
| AR-03 | 技术设计用“未来增加行政维修”做一次变更分析，说明哪些数据、页面、权限或流程会改变及如何兼容旧 IT 工单，不实际开发第二业务 |
| AR-04 | 将现有 IT 流程与权限用例保留为未来扩展的回归标准；目前只是验收要求，不声称已通过未来类型测试 |

推荐先清晰分模块的单体应用，不引入微服务以解决尚不存在的规模问题。数据表、接口、状态实现和技术组件选型由后续小范围技术设计明确；本次文档恢复不改变已有项目代码或技术栈。

## 8. 已确认决策与后续确认点

### 8.1 已确认决策（截至 2026-09-10）

| 编号 | 决策 | 取舍 |
| --- | --- | --- |
| D-00 | V1 只做 IT 问题，保留业务扩展边界 | 先闭环，不做多业务配置 |
| D-01 | 管理员人工分配，不抢单 | 职责清楚，少一套领取冲突规则 |
| D-02 | 追问只是回复，不设等待状态 | 操作简单，暂不统计等待时长 |
| D-03 | 申请人确认后关闭，管理员有理由代关，无自动关闭 | 真实确认优先，管理员承担例外处理 |
| D-04 | 关闭前允许未解决退回同负责人，记录追加；终态不重开 | 保留真实反馈，不改写历史 |
| D-05 | 仅待处理、处理中可有理由转派；处理中转派回 OPEN；已解决待确认不可转派 | 保留实际解决人及历次成果，不因长期未确认或停用而改写归属 |
| D-06 | 历任负责人能读整单及后续记录，不能以旧负责人身份写 | 保留上下文，当前责任唯一；申请人关系例外，停用不再可读 |
| D-07 | 管理员单建、Excel 批量员工、密码管理与停用；支持截图 | 不引入注册、统一登录或任意附件 |
| D-08 | 知识库、通知、Agent、通用审批等后置 | 限制 V1 工作量 |
| D-09 | 按明确条件取消，不新增取消审批状态 | 使用回复与理由留痕，事实由人工核实 |
| D-10 | 工程师可直接停用，未结束工单由管理员按原状态权限人工跟进；申请人停用后按服务必要性继续/取消 | 2026-09-10 取消强制先交接限制；不物理删除、不自动交接、不绕过转派或关闭条件 |

2026-09-10 另确认：底层五种状态、页面六种情况；历任负责人整单只读。系统负责最基本的数据、状态与权限约束，业务必要性和理由真实性由管理员判断，不以复杂审批替代人工管理。

### 8.2 下一阶段再确认，不阻塞本次恢复

先复核本文恢复后的业务规则与实施默认值，再讨论数据模型、接口、事务及并发方式、附件存储、密码交付细节与本地运行环境。对于会改变用户操作或验收结果的技术取舍先讨论，不默默扩需求。

页面评审不等于批准生产实现；已确认规则无须每次重新开放讨论，若更改需同时更新需求、验收及页面说明。

## 9. 建议开发与验证顺序

1. 环境与工程启动：由项目负责人搭建并理解框架、运行入口和本地版本管理。启动或演示接口成功只代表环境可用。
2. 第一条纵向链：初始管理员、账号创建、登录、创建工单、我的工单、创建历史及权限验证，连通真实前后端与数据库。
3. 分配与处理：管理员分配、工程师开始、普通回复追问、转派、旧负责人只读及冲突保护。
4. 业务闭环：解决、申请人未解决反馈、再次解决、确认关闭、管理员代关、取消与终态只读。
5. 补齐账号与截图：Excel、改密/重置/停用、附件上传与权限、失败处理；基础账号安全不能拖到交付后。
6. 完整验收：逐项记录 AC 结果，验证重启持久化、异常回滚和并发；补运行说明和已知限制。

按小步端到端功能推进，不先写完全部前端或全部后端。助手提供说明、评审和排障支持，核心代码与框架搭建仍由项目负责人亲自完成，除非另行要求代写。

## 10. 参考依据与已知限制

本稿以项目共同确认的业务决策为准，不照搬成熟系统的全部功能。

历史参考包括 [易维帮助台相关工单资料](https://www.ewei.com/p3/article?id=396920) 和 [蓝鲸 ITSM 权限说明](https://github.com/TencentBlueKing/bk-itsm/blob/develop/docs/install/permission_description.md)。它们用于此前理解生命周期与资源/动作权限；本次恢复未重新核对网页，不据此断言这些产品的当前功能或市场普遍做法。

已知限制：没有找到可逐字节还原的 0.6.1 历史文件；本稿重建结构和验收表述。现存页面评审记录是交叉校对依据，但其中草图检查不能证明正式账号、后端安全、数据库、Excel 和附件已完成。所有验收通过结论必须有真实执行证据。

## 11. 版本与恢复记录

| 版本 | 日期 | 主要变化 |
| --- | --- | --- |
| 0.1 | 2026-09-03 | 初始草案，尚含旧等待分支与较小账号范围 |
| 0.2 | 2026-09-06 | 确认 IT 范围及后续扩展边界，补架构验收要求 |
| 0.3 | 2026-09-06 | 确认申请人关闭、管理员有理由代关及不自动关闭 |
| 0.4 | 2026-09-06 | 增加取消路径、CANCELLED 与对应验收 |
| 0.5 | 2026-09-06 | 补 Excel 员工导入、密码/停用规则、截图及对应验收 |
| 0.6 | 2026-09-06 | 去除等待用户状态，明确追问不阻断、旧负责人整单只读 |
| 0.6.1 | 2026-09-06 | 补齐取消定义及 BR-11 对停用申请人无需服务条件的引用，对齐页面评审 |
| 0.6.1 内容恢复 | 2026-09-10 | 按已确认记录及现存页面评审重建正文；整理为 11 REQ、75 AC、14 BR、4 AR，规范 Markdown 换行；原文件已备份，不声称字节级历史恢复 |
| 0.7 | 2026-09-10 | 按本轮确认取消工程师停用前强制交接；明确五种状态/六种页面情况、仅待处理与处理中有理由转派、已解决待确认不可转派、实际解决人保留与历任负责人只读；修正权限验收前提及并发停用条件，保持 75 项 AC 编号 |

以上历史概述来自已有确认与评审记录，不是从 Git 历史恢复；当前仓库尚未建立可用于恢复旧版本的提交记录。本次只恢复工作区文档，不自动暂存或提交。
