# 工单系统 V1 技术设计与开发任务拆分

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 0.4 |
| 日期 | 初版 2026-09-12；本次更新 2026-09-23 |
| 需求依据 | 《工单系统 V1 需求规格与验收标准（草案）》0.7 |
| 当前范围 | 账号设计、开发库与建表执行方式、手工约束测试记录 |
| 状态 | sys_user 已建表，本轮唯一及 CHECK 约束测试通过；其他边界测试待补，后端数据库连接及账号功能未完成 |

## 1. 协作方式与进度

整体阶段、大任务、小任务及总体进度统一见同目录《工单系统_V1开发计划与进度.md》。本文件继续保存具体技术设计、操作说明和验证证据；下文 A-01～A-04 对应计划 P1 的账号表工作，A-05 对应 P2 的连接工作，不另起一套任务。

项目负责人亲手搭建框架、编写核心代码与 SQL；助手负责共同设计、解释、检查及排障。每次安排一个可验证的小任务，不一次生成整套系统。

目前已完成最小 Spring Boot 工程、GET /api/hello 测试接口及本地提交（663ec9f）；项目已推送 GitHub，日常开发分支为 dev/luoyu。接口曾验证 HTTP 200，现有上下文加载测试曾通过；这不代表账号或工单业务验收通过，也不保证服务当前正在运行。

本文件随进度逐步补充，不是已完成的全量技术设计。需求文档负责业务范围与验收，本文件负责实现设计及小任务。

## 2. 账号设计：已确认

### 2.1 三种角色共用一张账号表

员工、工程师和管理员共用一张账号表，通过角色字段区分，不分别建立员工表、工程师表和管理员表。一个账号只有一个固定角色，V1 不支持角色修改。

三种角色都可以提交自己的工单。“申请人、当前负责人、历任负责人”是工单与账号的关系，不是账号角色，也不作为账号表的三个字段。相关关系后续随工单模型设计。

### 2.2 最小逻辑字段

下表中的业务含义与英文列名已确认，物理类型、长度与约束见第 3 节。

| 字段 | 列名 | 含义及约束 |
| --- | --- | --- |
| 账号 ID | id | 系统生成的内部唯一标识，用于关联工单、回复和历史；不能用姓名关联 |
| 工号 | employee_no | 必填、唯一，作为登录账号；按文本保存，保留前导零；停用账号仍占用原工号 |
| 姓名 | name | 必填，用于页面展示和署名；允许重名 |
| 密码哈希 | password_hash | 保存密码的安全哈希，不保存明文密码，不向普通业务接口返回 |
| 角色 | role | 员工、工程师、管理员三者之一 |
| 账号状态 | status | 正常或停用；停用账号不可登录或继续访问 |
| 强制改密标记 | must_change_password | 表示是否必须先修改临时密码；与角色、账号状态相互独立 |
| 创建时间 | created_at | 系统填写账号创建时间 |
| 邮箱 | email | 可选联系信息，不作为本期登录账号或身份验证依据 |

共 8 项必要逻辑字段及 1 项可选邮箱。不增加部门、手机号、工程师技能、等级或所属组；管理员无需专属资料字段。后续若确有需求，再讨论增加。

### 2.3 已确认取值

| 字段 | 取值 | 含义 |
| --- | --- | --- |
| role | EMPLOYEE | 员工 |
| role | ENGINEER | 工程师 |
| role | ADMIN | 管理员 |
| status | ACTIVE | 正常 |
| status | DISABLED | 停用 |
| must_change_password | true | 必须先修改密码 |
| must_change_password | false | 无强制改密要求 |

true/false 是逻辑含义，建表声明使用 BOOLEAN，见第 3 节。账号的 ACTIVE 不代表可以绕过首次改密，也不表示具备全部业务权限。

### 2.4 状态与密码行为

- 创建账号：分配明确角色，初始状态正常，设置独立临时密码的哈希，强制改密标记为 true。
- 管理员重置密码：更换临时密码哈希，标记为 true，并使旧会话失效；不改变账号角色或将停用账号自动恢复正常。
- 完成规定的改密：更新密码哈希，标记改为 false；未完成前只允许必要的改密与退出操作。
- 停用账号：状态改为 DISABLED，角色、账号 ID 和既有历史关联保留；旧会话不能继续访问。不物理删除账号。
- 工程师有未结束工单也可停用，由管理员按需求 0.7 人工跟进；不自动转派、结束工单或改写解决人。已解决待确认仍不能转派。
- 是否停用、是否继续服务及理由真实性由管理员判断；后端仍负责基本的权限与状态约束。

这些是必须实现的行为要求，不代表仅增加上述字段就已完成会话失效、密码安全或历史保留。

## 3. 账号表结构方案：已确认

### 3.1 表名与字段

表名 sys_user，三种角色共用。采用自增主键，不引入分布式 ID。以下字段类型、长度和默认值已由项目负责人确认，并已由用户在开发库执行建表；实际结构与测试证据见第 5 节。

| 字段 | SQL 类型 | 约束与默认值 |
| --- | --- | --- |
| id | BIGINT | PRIMARY KEY、AUTO_INCREMENT |
| employee_no | VARCHAR(50) | NOT NULL、UNIQUE，无默认值 |
| name | VARCHAR(50) | NOT NULL，无默认值，允许重名 |
| password_hash | VARCHAR(255) | NOT NULL，无默认值 |
| role | VARCHAR(20) | NOT NULL，无默认值 |
| status | VARCHAR(20) | NOT NULL，DEFAULT 'ACTIVE' |
| must_change_password | BOOLEAN | NOT NULL，DEFAULT TRUE |
| created_at | DATETIME | NOT NULL，DEFAULT CURRENT_TIMESTAMP |
| email | VARCHAR(254) | 可空，未填写存 NULL，不设唯一约束 |

### 3.2 约束与实现边界

- 工号使用字符串，保留前导零并允许 IT001 等形式；停用后仍保留唯一约束，不复用工号。账号 ID 用于内部关联，姓名仅用于展示，不能用姓名识别人。
- role、status 使用可读字符串，后端以 Java 枚举表达；数据库使用 CHECK 约束合法取值，role 仅允许 EMPLOYEE、ENGINEER、ADMIN，status 仅允许 ACTIVE、DISABLED。不新增角色表。
- 创建账号必须明确提供密码哈希与角色，不能依赖统一默认密码或默认角色。Excel 员工导入由后端明确指定 EMPLOYEE，不能信任上传者自行授予管理权限。
- created_at 仅表示创建时间，默认由数据库生成，不随修改自动更新。服务端与数据库的时间约定在环境配置阶段统一。
- NOT NULL 不等于非空白。工号和姓名去除首尾空白后不能为空；后端校验长度，填写邮箱时校验基本格式。密码先安全哈希再保存。
- 停用和强制改密的业务权限由后端实现，不能因为表中存在状态字段就认为这些行为已经完成。
- 本期不加手机号、部门、头像、工程师技能或等级、删除标记；操作审计后续单独设计，不塞进账号表。

### 3.3 已落实与待落实细节

项目负责人已编写并执行 CREATE TABLE，文件为 backend/sql/001_create_sys_user.sql。本文件不另复制一份完整建表 SQL，避免两份定义不一致；本次仅更新文档，不修改 SQL 或连接数据库。

用户提供的输出确认开发库为 it_ticket_system，服务端为 MySQL 8.4.10，表使用 InnoDB、utf8mb4、utf8mb4_0900_ai_ci。当前排序规则比较字符串时不区分大小写，后续登录查询需与工号唯一性保持一致；大小写变体尚未实测。BOOLEAN 在实际表结构中显示为 tinyint(1)，并已通过 CHECK 拒绝值 2。

建表文件当前依赖开发库默认字符集和排序规则，跨环境执行前必须按第 5 节准备库并检查结果。角色/状态 CHECK 也受字符串比较规则影响，当前测试只证明 BOSS、UNKNOWN 被拒绝，未证明小写角色值被拒绝。后端应按约定枚举处理，不把数据库检查等同于完整输入校验。

密码哈希算法、会话方案和账号接口在相关任务中再设计；不自行发明加密算法。姓名展示与历史署名保留的具体实现随历史模型确定，不假定当前账号姓名就是永久历史快照。

## 4. 小任务清单

| 任务 | 操作及产出 | 验证方式 | 状态 |
| --- | --- | --- | --- |
| A-01 账号逻辑设计 | 确认一张表、9 个字段及三组取值 | 对照需求 0.7，检查角色与工单关系没有混淆 | 已确认，已记录 |
| A-02 账号表物理设计 | 确定 sys_user 的类型、长度、主键与约束 | 与用户提供的 SHOW CREATE TABLE 输出核对 | 已确认、已核对结构 |
| A-03 手写建表 SQL | 用户编写并在开发库执行 | 已读取 SQL 文件，用户输出证明表已创建 | 已完成 |
| A-04 验证表结构 | 虚构数据验证正常插入、唯一与 CHECK 约束及回滚 | 用户提供完整输出，见第 5 节；前导零、必填/可空等待补测 | 本轮约束测试通过，非全量验收 |
| A-05 后端数据库连接 | 用户配置必要依赖及本地连接，助手讲解与检查 | 后续验证连接和简单查询，密码不进入 Git | 未开始；当前节点文档及 SQL 检查提交后推进 |

完成并验证一个小任务后，再检查 Git 差异、提交到本地并推送开发分支；是否合并 main 取决于该阶段验证结果。不自动提交未审查文件。

账号表之后的数据库连接、账号创建、登录与工单功能任务，待本轮设计完成后逐项拆分，不把它们当作一个立即完成的大任务。

## 5. 开发库、执行方式与验证记录

### 5.1 证据范围与环境

本节依据用户在本对话中提供的 MySQL 输出整理。2026-09-13 完成建表结构及连接编码核对；2026-09-21 提供完整唯一约束、CHECK 及回滚输出。本次文档更新没有重新登录数据库、执行测试或修改数据。

| 项目 | 已确认内容 |
| --- | --- |
| 服务端 | MySQL Community Server 8.4.10 |
| 开发数据库 | it_ticket_system |
| 表 | sys_user，9 个字段 |
| 表引擎 / 字符集 / 排序规则 | InnoDB / utf8mb4 / utf8mb4_0900_ai_ci |
| 连接编码 | 用户核对时 client、connection、results 均为 utf8mb4，SELECT 中文显示正常；重新连接后需按实际配置确认 |
| 建表来源 | backend/sql/001_create_sys_user.sql |
| 凭据 | 不记录密码；root 仅用于用户本机手工管理，应用连接账号及最小权限另行设计 |

### 5.2 首次准备与执行说明

以下是操作记录和后续新环境参考，不是要求在已有数据库重复执行。新建前先 SHOW DATABASES，建表前先 SHOW TABLES；若库或表已存在，先核对，不能为重跑脚本直接删除现有数据。

在 PowerShell 中：

```powershell
chcp 65001
mysql --default-character-set=utf8mb4 -u root -p
```

- chcp 65001：设置当前控制台代码页为 UTF-8，不修改数据库或系统全局设置。
- mysql：启动命令行客户端；--default-character-set 指定连接编码；-u 指定用户；-p 交互输入密码，不将密码写在命令行或文档中。

在 mysql> 中，仅在项目库不存在时执行：

```sql
CREATE DATABASE it_ticket_system
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_0900_ai_ci;
```

CREATE DATABASE 会新建库；CHARACTER SET 指定字符集，COLLATE 指定字符串比较规则，不改变其他已有数据库。

```sql
USE it_ticket_system;
SELECT DATABASE();
```

USE 为当前连接选择数据库；SELECT DATABASE() 只查询当前库。重新登录后需重新 USE，或在 mysql 登录命令末尾指定 it_ticket_system。测试中曾出现 ERROR 1046 No database selected，是未选择库，相关 INSERT 未成功，不是约束缺陷。

确认当前库正确且 sys_user 尚不存在后：

```sql
SOURCE E:/工单系统/backend/sql/001_create_sys_user.sql;
```

SOURCE 读取并执行文件，会创建表。文件未设置重复执行保护，已有表时不要反复执行。建库建表是 DDL，不能依赖后面的 ROLLBACK 撤销；第 5.3 节事务回滚仅用于测试数据的插入和更新。

以下命令只查看：

```sql
SHOW TABLES;
SHOW CREATE TABLE sys_user\G
SHOW VARIABLES LIKE 'character_set_%';
SELECT '中文测试' AS text_check;
```

- SHOW TABLES 列出当前库的表；SHOW CREATE TABLE 查看实际结构，\G 以纵向格式显示，后面不用再加分号。
- SHOW VARIABLES 查看字符集配置；SELECT 检查中文往返显示，AS 为结果列起别名。
- 实际结构中 BOOLEAN 显示为 tinyint(1)、TRUE 显示为 1。初次会话为 gbk，因此约束字面量带 _gbk 标记，但表仍为 utf8mb4，无需因此重建。
- 字符集查询输出曾带 1 warning，未取得 SHOW WARNINGS 内容，原因未确认；不据此推断表异常或声称已消除警告。

### 5.3 2026-09-21 手工约束测试

测试对象是虚构工号 TEST_CHECK_001，姓名“约束测试”，password_hash 使用 TEST_ONLY_NOT_A_REAL_HASH 占位文本。这只是表结构测试，不是真实密码哈希，不可用于登录或初始化真实账号。

测试步骤是在同一连接中选库、START TRANSACTION、执行插入及错误用例，最后 ROLLBACK 并查询剩余记录。START TRANSACTION 开始事务；INSERT 插入数据；UPDATE 修改数据，各条 UPDATE 都用 WHERE employee_no = 'TEST_CHECK_001' 限定范围；ROLLBACK 撤销事务中数据变更；COUNT(*) 统计剩余测试行。不执行 COMMIT，不混入 DDL。

| 测试 | 用户提供的实际结果 | 结论 |
| --- | --- | --- |
| 插入合法角色 EMPLOYEE 的测试账号 | Query OK, 1 row affected | 正常插入通过 |
| 再插入相同工号 TEST_CHECK_001 | ERROR 1062，Duplicate entry，键 sys_user.employee_no | 工号唯一约束有效 |
| 将 role 改为 BOSS | ERROR 3819，chk_sys_user_role is violated | 非法角色被拒绝 |
| 将 status 改为 UNKNOWN | ERROR 3819，chk_sys_user_status is violated | 非法状态被拒绝 |
| 将 must_change_password 改为 2 | ERROR 3819，chk_sys_user_must_change_password is violated | 非 0/1 值被拒绝 |
| 执行 ROLLBACK | Query OK | 回滚命令成功 |
| 回滚后按工号查询 COUNT(*) | remaining_test_rows = 0 | 本次 TEST_CHECK_001 记录已清理，不代表整个库没有其他数据 |

中间四次错误是预期结果，不是程序故障。自增序号可能因插入或回滚产生间隔，不要求连续。

### 5.4 尚未验证及后续安排

- 早前默认值测试用户反馈“没有报错”，但未提供对应查询明细；SHOW CREATE TABLE 已证明默认值定义存在，不能据此声称各默认值的实际插入结果均已核验。
- 前导零工号的存取、工号大小写变体、同名账号、必填字段显式 NULL 拒绝、邮箱 NULL、字符串长度边界等尚未取得完整测试证据。
- 空白字符串、邮箱格式、真实密码哈希、登录、强制改密、停用会话失效、角色权限、并发以及工单业务尚未验证。建表和约束测试不代表 REQ-01/REQ-10 整体通过。
- 本次约束测试为用户手工执行、助手核对输出，没有新增自动化数据库测试。
- 当前先保存文档和 SQL，检查 Git 暂存差异后由用户提交并推送开发分支；其他既有改动不能顺手纳入。SQL 文件如存在暂存后继续修改，提交前需重新暂存最新版。
- 随后按小任务指导用户完成 Spring Boot 数据库连接配置与简单查询；不直接跨到完整登录或批量导入。

## 6. 版本记录

| 版本 | 日期 | 变化 |
| --- | --- | --- |
| 0.1 | 2026-09-12 | 记录已确认的账号逻辑字段及枚举，区分物理设计待定事项，拆分第一张表的小任务；未修改业务代码或数据库 |
| 0.2 | 2026-09-12 | 记录已确认的 sys_user 表名、9 个字段的类型/长度/默认值、主键及唯一与合法值约束；更新任务进度，保留用户手写 SQL，尚未执行数据库验证 |
| 0.3 | 2026-09-21 | 据用户输出记录 MySQL 环境、建库/建表与连接编码步骤、唯一及三个 CHECK 约束测试和回滚结果；更新进度并明确未测范围，仅修改文档 |
| 0.4 | 2026-09-23 | 增加整体开发计划入口及任务编号对应关系；原设计与测试证据保持不变 |
